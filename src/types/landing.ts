export interface LandingColors {
  bg: string
  fg: string
  card: string
  card2: string
  border: string
  muted: string
  nav: string
}

export interface ModalColors {
  bg: string
  border: string
  muted: string
  fg: string
  input: string
}
